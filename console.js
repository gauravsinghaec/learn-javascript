const y = 10;
let z = 20;

// Pring the values of y, y
console.log(z, y);

// Print the value with variable name
console.log(`y = ${y} and z = ${z}`);
console.log("y = ", y, "and z = ", z);
console.log("y = " + y + " and z = " + z);

// We can print variable as object
console.log({ y, z });

let text = "Delhi Election";
console.log("the text is ", text);

console.log({ text: text });

const obj1 = { text };
